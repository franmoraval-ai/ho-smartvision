api:
  listen: ":1984"
rtsp:
  listen: ":8554"
webrtc:
  listen: ":8555"

streams:
  # Cámara del panel (id en Supabase) -> cámara pública REAL de tráfico.
  # Cámara de tráfico del condado de Arlington (Virginia, EE.UU.), "Glebe Rd at Washington Blvd".
  # Publicada oficialmente por el gobierno del condado para información de tráfico (legítima).
  # Vídeo H264 en vivo vía HLS, transcodificado a H264 limpio para reproducción fluida.
  # El nombre DEBE coincidir con el id de la cámara en Supabase para que el panel lo muestre.
  "44444444-4444-4444-4444-444444444444": ffmpeg:https://itsvideo.arlingtonva.us:8011/live/cam10.stream/playlist.m3u8#video=h264

  # Alias adicional de la misma cámara pública (por si quieres abrirla por nombre).
  "camara-publica": ffmpeg:https://itsvideo.arlingtonva.us:8011/live/cam10.stream/playlist.m3u8#video=h264

  # Patrón de PRUEBA sintético (por si necesitas validar el pipeline sin red externa).
  "test-pattern": ffmpeg:virtual?video=testsrc&audio=sine#video=h264#audio=opus

  # Cámara pública de COSTA RICA: "Santa Teresa Live Surf Cam 24/7" (YouTube, transmisión pública).
  # yt_source.py resuelve la URL HLS con yt-dlp y go2rtc la transcodifica a H264 limpio.
  "costa-rica": echo:py "C:\Users\marco\OneDrive\Desktop\Ho smartvision\gateway\yt_source.py" cCIpDLrYKsU
